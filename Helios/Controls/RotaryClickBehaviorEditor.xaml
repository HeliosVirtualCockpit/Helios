<HeliosSdk:HeliosPropertyEditor x:Class="GadrocsWorkshop.Helios.Controls.RotaryClickBehaviorEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:Helios="clr-namespace:GadrocsWorkshop.Helios"
             xmlns:HeliosBase="clr-namespace:GadrocsWorkshop.Helios.Controls"
             xmlns:HeliosSdk="clr-namespace:GadrocsWorkshop.Helios.Windows.Controls"                      
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             d:DesignWidth="180">

    <HeliosSdk:HeliosPropertyEditor.Resources>
        <Helios:EnumConverter Type="{x:Type HeliosBase:ClickControlType}" x:Key="ClickControlTypes" />
        <Helios:EnumConverter Type="{x:Type HeliosBase:ToggleSwitchLockPosition}" x:Key="ToggleSwitchLockPosition" />
        <Helios:EnumConverter Type="{x:Type HeliosBase:RotaryClickAllowRotationType}" x:Key="AllowRotationTypes" />

        <Style x:Key="ShowIfClickConfigurable" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Control.ClickConfigurable}" Value="true">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ShowIfSwitch" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Control.IsSwitch}" Value="true">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ShowIfPushButton" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Control.IsSwitch}" Value="false">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

    </HeliosSdk:HeliosPropertyEditor.Resources>
    <Grid Background="Transparent" Margin="4"  Style="{StaticResource ShowIfClickConfigurable}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="80" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Margin="4,10,2,2" Style="{StaticResource Heading2}"><Run Language="en-gb" Text="Click"/></TextBlock>
        <Label Grid.Column="0" Grid.Row="1" FontSize="10" HorizontalAlignment="Right" Content="Emulation">
            <Label.ToolTip>The way that the click acts (eg a push button or a switch).</Label.ToolTip>
        </Label>
        <ComboBox Grid.Column="1" Grid.Row="1" FontSize="10" Margin="0,2,0,2" HorizontalAlignment="Stretch" ItemsSource="{Binding Source={StaticResource ClickControlTypes}, Path=DisplayNames}" SelectedItem="{Binding Path=Control.ClickControlType, Converter={StaticResource ClickControlTypes}}" />
        <Label Grid.Column="0" Grid.Row="2" FontSize="10" HorizontalAlignment="Right" Content="Rotation Lock"  Style="{StaticResource ShowIfSwitch}">
            <Label.ToolTip>The way that the click acts (eg a push button or a switch).</Label.ToolTip>
        </Label>
        <ComboBox Grid.Column="1" Grid.Row="2" FontSize="10" Margin="0,2,0,2" HorizontalAlignment="Left" ItemsSource="{Binding Source={StaticResource ToggleSwitchLockPosition}, Path=DisplayNames}" Style="{StaticResource ShowIfSwitch}" SelectedItem="{Binding Path=Control.LockPosition, Converter={StaticResource ToggleSwitchLockPosition}}" />
        <Label Grid.Column="0" Grid.Row="3" FontSize="10" HorizontalAlignment="Right" Content="Pushbutton Rotation"  Style="{StaticResource ShowIfPushButton}">
            <Label.ToolTip>Select types of knob rotation to be allowed.</Label.ToolTip>
        </Label>
        <ComboBox Grid.Column="1" Grid.Row="3"  FontSize="10" Margin="0,2,0,2" HorizontalAlignment="Stretch" ItemsSource="{Binding Source={StaticResource AllowRotationTypes}, Path=DisplayNames}" Style="{StaticResource ShowIfPushButton}" SelectedItem="{Binding Path=Control.AllowRotation, Converter={StaticResource AllowRotationTypes}}" />

    </Grid>
</HeliosSdk:HeliosPropertyEditor>
